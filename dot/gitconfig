[advice]
	statusHints = false
	detachedHead = false

[alias]
	a      = add
	b      = branch --verbose --verbose
	d      = diff
	ds     = diff --stat
	initm  = "!f() { cd ${GIT_PREFIX:-.}; git init --template= && git commit --allow-empty --message \"$*\"; }; f"
	j      = jump diff
	lca    = "!f() { git merge-base ${1:-master} ${2:-HEAD} | cut -b-7; }; f"
	p      = push
	pr     = "!f() { git remote | xargs -n1 git push --verbose --tags; }; f"
	tidy   = "!f() { git fsck --full --strict; git gc --aggressive; }; f"
	when   = log --diff-filter=A

	# commit
	cam    = "!f() { git commit --all --message \"$*\"; }; f"
	camp   = "!f() { git commit --all --message \"$*\"; X=$(git remote); [ -n \"$X\" ] && git push; }; f"
	cm     = "!f() { git commit --message \"$*\"; }; f"

	# hiding
	hidden = "!f() { git ls-files -v | sed -n '/^[a-z]/{s/^..//;p}'; }; f"
	hide   = update-index --assume-unchanged
	unhide = "!f() { git update-index --no-assume-unchanged; }; f"

	# logs
	l      = log --branches --graph --abbrev-commit --pretty=tidy -n 30
	ll     = log --branches --graph --abbrev-commit --pretty=tidy
	ln     = log --branches --graph --abbrev-commit --pretty=named -n 30
	ls     = log --branches --graph --abbrev-commit --pretty=tidy --stat

	# rebase
	rb     = rebase --committer-date-is-author-date
	ri     = rebase --committer-date-is-author-date --interactive --autosquash
	fix    = "!f() { git commit --fixup  $(git log -n 1 --format='%h' --grep \"^$@\"); }; f"
	sq     = "!f() { git commit --squash $(git log -n 1 --format='%h' --grep \"^$@\"); }; f"

	# stash
	pop    = stash pop
	save   = stash save

	# status
	s      = "!f() { git stash list; git status --short \"$@\"; }; f"
	sb     = "!f() { git branch --verbose --verbose; git stash list; git status --short; }; f"
	st     = status

[color "branch"]
	current = green
	local   = white
	remote  = cyan

[color "diff"]
	meta       = green
	frag       = cyan
	old        = red
	new        = yellow
	whitespace = red reverse

[color "status"]
	added     = yellow
	changed   = green
	untracked = cyan

[color]
	branch = auto
	diff   = auto
	status = auto

[commit]
	template = ~/.gitmessage

[core]
	whitespace     = blank-at-eol,space-before-tab,indent-with-non-tab,tabwidth=8,-blank-at-eof
	pager          = "less -eFfiqrX"
	preloadindex   = true
	attributesfile = ~/.gitattributes
	excludesfile   = ~/.gitignore

[diff "xz"]
	textconv = xz --decompress --stdout

[difftool]
	prompt = no

[diff]
	mnemonicprefix = true
	tool           = vimdiff
	guitool        = vimdiff

[github]
	user  = flatcap
	token = 87f49c3aebb08f1add7b59c1e3f57cd0

[gpg]
	program = gpg2

[grep]
	# lineNumber = true
	extendedRegexp = true

[help]
	autocorrect = -1

[include]
	# path = ~/.gitconfig-private

[init]
	# templatedir = ~/.git_template

[interactive]
	singlekey = true

[log]
	abbrevCommit = true
	date = relative

[mailmap]
	file = ~/.gitmailmap

[mergetool]
	prompt = false

[merge]
	tool    = vimdiff
	guitool = vimdiff

[pretty]
	tidy  = %Cred%h%Creset %s %Cgreen(%cr)%Creset%C(yellow)%d%Creset
	named = %Cred%h%Creset %s %Cgreen(%cr)%Creset%C(yellow)%d%Creset %C(white dim)%an <%ae>%Creset

[push]
	default = matching

[user]
	email = rich@flatcap.org
	name = Richard Russon
	signingkey = Richard Russon (Code Signing) <rich@flatcap.org>

