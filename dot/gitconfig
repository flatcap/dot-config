[user]
	email = richard.russon@gmail.com
	name = Richard Russon (flatcap)

[github]
	user  = flatcap
	token = 87f49c3aebb08f1add7b59c1e3f57cd0

[push]
	default = matching

[alias]
	l    = log --graph --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cd)%Creset%C(yellow)%d%Creset' --abbrev-commit --date=relative -n 20
	ll   = log --graph --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cd)%Creset%C(yellow)%d%Creset' --abbrev-commit --date=relative
	ls   = log --graph --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cd)%Creset%C(yellow)%d%Creset' --abbrev-commit --date=relative --stat
	s    = !git stash list --date=local && git status --short
	st   = status
	d    = diff
	ds   = diff --stat
	b    = branch --verbose
	tidy = !git prune && git gc --aggressive && git fsck --full --strict
	lca  = !git merge-base master $(git rev-parse --abbrev-ref HEAD) | cut -b-7
	since= log --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cd)%Creset' --abbrev-commit --date=relative --since

[color]
	branch = auto
	diff   = auto
	status = auto

[color "branch"]
	current = green
	local   = white
	remote  = cyan

[color "diff"]
	meta       = green
	frag       = cyan
	old        = red
	new        = yellow
	whitespace = red reverse

[color "status"]
	added     = yellow
	changed   = green
	untracked = cyan

[core]
	whitespace   = blank-at-eol,space-before-tab,indent-with-non-tab,tabwidth=8,-blank-at-eof
	pager        = "less -eFfiqrX"
	excludesfile = /home/flatcap/.gitignore

[diff]
	mnemonicprefix = true
	tool           = vimdiff
	guitool        = vimdiff

[difftool]
	prompt = no

[merge]
	tool    = vimdiff
	guitool = vimdiff

[gpg]
	program = gpg2

[rich]
	search = "(QWQ|RAR)"
