[user]
	email = richard.russon@gmail.com
	name  = Richard Russon (flatcap)

[github]
	user  = flatcap
	token = 87f49c3aebb08f1add7b59c1e3f57cd0

[push]
	default = matching

[alias]
	l    = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cd)%Creset' --abbrev-commit --date=relative
	ls   = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cd)%Creset' --abbrev-commit --date=relative --stat
	s    = !git stash list && git status -s
	st   = status
	sl   = !git status && echo && git l -n 4
	d    = diff
	ds   = diff --stat
	b    = branch --verbose
	tidy = !git prune && git gc --aggressive && git fsck --full --strict

[color]
	branch = auto
	diff   = auto
	status = auto

[color "branch"]
	current = green
	local   = white
	remote  = cyan

[color "diff"]
	meta       = green
	frag       = cyan
	old        = red
	new        = yellow
	whitespace = red reverse

[color "status"]
	added     = yellow
	changed   = green
	untracked = cyan

[core]
	whitespace   = trailing-space,space-before-tab,indent-with-non-tab,tabwidth=8
	pager        = "less -eFfiqrX"
	excludesfile = /home/flatcap/.gitignore

[diff]
	mnemonicprefix = true

[giggle]
	main-window-maximized = true
	main-window-geometry = 0x0+0+0
	history-view-vpane-position = 399
	main-window-view            = HistoryView
	file-view-vpane-position    = 214

