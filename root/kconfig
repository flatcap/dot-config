#!/bin/bash

make modules_install
make install

head -3 Makefile | sed 's/ //g' > jim

. jim
. .config

rm -f jim

cp .config /boot/config-$VERSION.$PATCHLEVEL.$SUBLEVEL$CONFIG_LOCALVERSION

chmod og+r /boot/*$VERSION.$PATCHLEVEL.$SUBLEVEL$CONFIG_LOCALVERSION*
