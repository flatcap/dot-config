export UID
(umask 077; mkdir --parents /run/user/$UID/lvm /run/user/$UID/ssh)

export PROMPT_CFG_DIR_TYPE=work

#ulimit -c unlimited

stty -ixon -ixoff

export PATH=".:/home/flatcap/bin:/home/flatcap/bin/colour:/home/flatcap/bin/sudo:/usr/bin:/usr/local/bin:/usr/sbin"
export PATH="${PATH/\~/$HOME}"

source /usr/share/bash-completion/completions/systemctl
alias !=history2

